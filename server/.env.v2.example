# Anthony's Super Kitchen Telegram Bot v2
# Copy these names into the deployment secret/config store. Never commit real values.

PORT=3001

# REQUIRED before enabling the bot. Must be a freshly rotated BotFather token.
TELEGRAM_TOKEN=

# Customer Mini App and protected Edge Function.
MINIAPP_URL=https://jelfferyduran.github.io/anthony-supermarket/apps/superkitchen/dist/
MINIAPP_API_URL=https://cbpdiiyzzmbavsymjysb.supabase.co/functions/v1/superkitchen

# Optional. Used only to recognize the kitchen chat; order data/status changes are
# still intentionally handled by the authenticated KDS instead of Telegram.
KITCHEN_CHAT_ID=

# Intentionally absent from bot v2:
# - SUPABASE_SERVICE_ROLE_KEY
# - SUPABASE_ANON_KEY
# - database credentials
# - local order storage paths
